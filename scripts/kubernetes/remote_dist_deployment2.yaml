apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: remote-dist2
  name: remote-dist2
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: remote-dist2
  template:
    metadata:
      labels:
        run: remote-dist2
    spec:
      containers:
      - command: 
        image: docker.io/robertovrf/remote-dist:all
        name: remote-dist2
        ports:
        - containerPort: 2011
        - containerPort: 2010
        imagePullPolicy: Always
        env:
          - name: POD_ID
            valueFrom:
              fieldRef:
                apiVersion: v1
                fieldPath: metadata.uid
---
apiVersion: v1
kind: Service
metadata:
  name: remote-dist2
spec:
  ports:
  - port: 2011
    name: rdist
    targetPort: 2011
  selector:
    app: remote-dist2
  type: LoadBalancer
